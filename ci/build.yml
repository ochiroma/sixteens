---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 14.15.5

inputs:
  - name: sixteens

outputs:
  - name: build

run:
  path: sixteens/ci/scripts/build.sh
